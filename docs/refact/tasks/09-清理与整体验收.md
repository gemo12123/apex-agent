# 清理与整体验收任务

> 任务域：跨模块最终收口
> 当前总体进度：已完成（2026-08-04）；CLEAN-01～03 全部完成，最终重构交付收口

## CLEAN-01 删除 PlanExecutor、Stage 与执行模式

- **任务名称**：删除全部 PlanExecutor 运行概念和模式分支。
- **任务目标**：确保目标后端只有一个 ReAct 循环，`"react"` 仅作为协议兼容值存在。
- **当前进度**：已完成（2026-08-04）。新 platform 产品入口已由 `ChatController -> ChatService -> ApexAgentRuntime/ApexAgentExecution` 接管；旧 PlanExecutor、Stage、模式分支、计划工具、Plan Prompt 和事件生产者已随 legacy 删除，protocol 兼容 DTO 保留。
- **设计依据**：原始需求第 3 条；设计文档第 2.5～2.7、17 节；架构文档第 2.1、19.1～19.2 条。
- **涉及范围**：ModeEnum、executionMode、Stage/Plan 状态、StageToolResolver/Plan、ToolInterceptor 模式守卫、StagePromptBuilder 分支、Plan Prompt、WritePlanTool、UpdatePlanTool、TASK_THINK 生产逻辑、数据库字段/快照。
- **前置依赖**：CORE-05A～05C、CORE-06、KIT-03、RUN-08、PLAT-04。
- **具体执行内容**：
  1. 删除执行模式枚举、配置、上下文字段和模式分支。
  2. 删除 Plan、执行 Stage、计划工具、Prompt 和运行事件生产逻辑。
  3. 删除数据库 execution_mode/current_stage/plan snapshot 设计残留。
  4. 保留 protocol 中兼容 DTO 和前端现有消费代码。
  5. 用源码/资源/配置搜索确认无 legacy 运行引用。
- **预期产出**：无 PlanExecutor 的后端、删除清单和唯一循环证明。
- **验收标准**：
  - 后端源码、测试、配置和 Prompt 中无 PlanExecutor/执行模式/Stage 运行逻辑。
  - protocol 兼容 DTO 仍能通过序列化测试，但默认链路不生产。
  - 所有运行事件的 mode 仍固定 `"react"`，core/common 不定义 Mode。
  - 父工程测试通过。
- **限制条件或注意事项**：不得删除 protocol 兼容 DTO或修改前端；清理前必须证明新 ReAct 链路已覆盖旧入口。
- **完成证据**：删除前父 reactor 全量测试通过；core 唯一循环与 `mode=react` 测试通过；删除后 runtime 回归测试明确断言默认执行不产生 PLAN/TASK_THINK 兼容事件。详见 [CLEAN-01/02 清理报告](../evidence/CLEAN-01-02清理报告.md)。

## CLEAN-02 完成 ApexAgent 重命名与 legacy/Fastjson 清理

- **任务名称**：统一后端命名并删除被新模块替代的旧实现。
- **任务目标**：最终产物不再保留 SuperAgent 概念、重复 Hook Runtime、死 Adapter 或 Fastjson。
- **当前进度**：已完成（2026-08-04）。目标链路已统一为 ApexAgent 命名；legacy、迁移期 architecture-tests、重复 Hook Runtime、死 Adapter、Fastjson/Fastjson2 调用与依赖均已删除，父 reactor 只保留八个目标模块。
- **设计依据**：原始需求第 5 条；设计文档第 18、20 节阶段 8；架构文档第 16、19.14 条。
- **涉及范围**：SuperAgent* 类型、日志/异常/注释/测试、重复 AgentHookRuntime、临时 Adapter、旧单模块包、Fastjson 依赖/import、过时配置。
- **前置依赖**：CLEAN-01、全部模块迁移任务、COM-04。
- **具体执行内容**：
  1. 按设计映射完成 ApexAgent、ApexAgentContext、ApexAgentCoordinator、ApexAgentPlatformConfiguration 等命名。
  2. 删除由新 core/runtime/platform 替代的 SuperAgentFactory、SessionService、旧 Hook Runtime 和死代码。
  3. 清理临时 Adapter，删除 `legacy` 模块并从父 POM 移除；最终只保留八个目标模块。
  4. 删除 Fastjson/fastjson2 依赖和调用，统一使用 JsonUtils。
  5. 更新 JavaDoc、日志、异常、Prompt 和测试类名中的后端 SuperAgent 文字。
  6. 对每个模块执行 dependency analyze，清除 used-but-undeclared；确认 protocol 测试不依赖 common，kit 不含 `StandardToolResultFactory`。
- **预期产出**：无 legacy 实现和双 JSON 栈的八模块源码树。
- **验收标准**：
  - 生产源码搜索无 `class SuperAgent`、执行模式和 Fastjson import。
  - Maven dependency tree 无 Fastjson/fastjson2。
  - 无额外 legacy Maven 模块，重复 Hook 调度实现为 0。
  - 各模块直接依赖与目标图一致，无 reactor 回边；拒绝/强制结束/取消 ToolResult 只由 core 唯一工厂生成。
  - 外部 URL、sessionId、agentKey、SSE 事件名和仓库目录名未改变。
- **限制条件或注意事项**：不得机械删除 protocol 兼容词或历史设计文档；只更新当前态文档。删除必须在对应新实现测试通过后进行。
- **完成证据**：父 reactor、七个代码模块依赖分析、Fastjson 依赖树和八模块结构守卫通过；memory 保持无标准源码的 `packaging=pom` 占位模块。详见 [CLEAN-01/02 清理报告](../evidence/CLEAN-01-02清理报告.md)。

## CLEAN-03 更新当前态文档并执行全量交付验收

- **任务名称**：完成架构、行为、恢复、平台和前端兼容的最终验收。
- **任务目标**：用可复现证据证明全部设计目标已实现，并同步当前态文档。
- **当前进度**：已完成（2026-08-04）。当前态 reference/overview/spec、启动部署验证与发布回滚说明已更新；父 reactor、架构、core、runtime、platform 非 PG、memory 静态和前端验收通过。PostgreSQL/Testcontainers 因无 Docker 1 项未运行，按本轮用户明确环境豁免接受但不记为通过。
- **设计依据**：设计文档第 20 节阶段 8、第 21～24 节；架构文档第 17～19 节；仓库 `AGENTS.md` 完成标准。
- **涉及范围**：`docs/reference`、`docs/overview`、`docs/spec`、发布说明、父工程测试、模块架构测试、PostgreSQL 集成测试、前端验证。
- **前置依赖**：CLEAN-01/02、FND-03B、PRO-02、CORE-07C、RUN-08、PLAT-04、MEM-03；Q-01～Q-17 已按任务 README 的确认结果落实。
- **具体执行内容**：
  1. 更新后端开发、跨端契约、验证交付、项目概览、快速开始、架构流程和消息标准等当前态文档。
  2. 运行父 POM 全量测试和自动架构测试。
  3. 运行 core 生命周期/ReAct/恢复、runtime 无 Spring、platform PostgreSQL/HTTP/SSE 集成测试。
  4. 运行请求级取消矩阵：模型/MCP/HTTP 主动取消、emitter callback 竞态、runtime close 与活动登记竞态，以及 finally 前 lease 不释放。
  5. 按 Q-14 的最小前端改动范围运行 `test:run`、`typecheck`、`build`，确认既有 chat/SSE 无回归。
  6. 验证 Spring AI 对齐报告、前后 dependency tree、Enforcer convergence、R-13 豁免清零以及 Spring Boot/模型供应商 SDK 版本未变化。
  7. 生成覆盖矩阵、测试结果、未验证项、默认能力变化和单实例限制说明。
- **预期产出**：当前态文档、全量验证记录、发布/回滚注意事项和最终覆盖矩阵。
- **验收标准**：
  - 七个代码模块编译测试，memory 以无依赖 `packaging=pom` 参与父 reactor 且 archive 不进入构建。
  - FND-03B 全部架构规则通过。
  - Spring AI 关键 artifact 各只有一个 resolved 版本；每项版本变化都有 Q-17 触发证据，父 POM 是唯一所有者，Spring Boot/模型供应商 SDK 未调整。
  - core 11 生命周期、ReAct、多工具、压缩、恢复五分支测试通过。
  - runtime 无 Spring IoC 执行、事件隔离、lease 和资源关闭测试通过；RUNNING cancel/close 只发命令并立即返回，默认 adapter 主动取消，run finally 才释放 lease，且无超时/grace period。
  - platform 协议、409、独立 emitter、PostgreSQL TEXT、Repository 提交顺序和重启恢复测试通过；不要求跨 Repository 原子回滚。
  - memory 归档 MANIFEST 完整且不在默认依赖、source/resource 或发布物中；报告明确未编译、未测试 memory。
  - 前端三项验证通过，刷新后可重新展示两类 HITL 卡片，既有 chat/SSE 行为无回归。
- **限制条件或注意事项**：不得把未运行的检查写成通过；不得把本期单实例描述为水平扩展安全；未关闭的关键设计问题必须列为阻塞，不可用实现假设掩盖。
- **完成证据**：详见 [CLEAN-03 最终交付验收报告](../evidence/CLEAN-03-最终交付验收报告.md)。最终 `clean verify` 共 182 项、0 失败、0 错误、1 项 PostgreSQL 环境跳过；前端 48 项及 typecheck/build 通过。
